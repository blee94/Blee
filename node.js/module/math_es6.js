// const add = (a, b) => a + b;
// const minus = (a, b) => a - b;

// // 하나만 내보낼 경우
// export default add;

// 여러개 내보낼 경우
// export { add, minus };

// const math = (a, b) => {
//   let add = a + b;
//   let minus = a - b;
// };

function math(a, b) {
  const add = a + b;
}
const PI = 3.14;
export default math;
// export default PI;
