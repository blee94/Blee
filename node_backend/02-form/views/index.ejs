<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  </head>
  <body>
    <!-- ---------get 요청 -->
    <h1>로그인</h1>
    <!-- form 전송은 클라이언트가 서버로 데이터를 보내는 방법 중 하나. -->
    <!-- action: 요청을 보낼 주소 (제 서버에 보낸다는 가정하에 도메인을 뗀 나머지 주소)-->
    <!-- method: 어떤 방식의 요청을 사용할지 (get or post) -->
    <form action="/get" method="get">
      <input type="text" name="id">
      <input type="password" name="pw">
      <!-- 버튼 타입이 submit인 경우 버튼을 감싸고 있는 form태그의 정보에 맞게 폼을 제출함. -->
      <button type="submit">제출</button>
    </form>
    <h1>회원가입</h1>
    <form action="/get" method="get">
      id: <input type="text" name="id">
      pw: <input type="password" name="pw">
      <!-- label -->
      <input type="radio" name="gender" value="male" id="men"><label for="men"> 남자 </label>

      <input type="radio" name="gender" value="female">여자
      <button type="submit">회원가입</button>
    </form>
    <!-- ---------post 요청 -->
    </form>
    <h1>회원가입</h1>
    <form action="/post/ver2" method="post">
      <input type="text" name="name" pattern="^([a-zA-Z0-9가-힝]){5, }$" title="소문자, 대문자, 한글, 숫자로 조합된 5~20자리 문자열">
      <input type="password" name="pw">
      <input type="email" name="email" oninput="validCheck(this)">
      <button type="submit">제출</button>
    </form>
    <script>
      function validCheck(el) {
        const value = el.value;
        const regexEm = /^([a-zA-Z0-9가-힝]){5, }$/
        // regexEm.test('검사하고 싶은 값') => true or false 를 return
        if (regexEm.test(value)) {
          console.log('정규식 일치')
        } else {
          console.log('정규식 불일치')
        }

      }

      var regType1 = /^[A-Za-z0-9]*$/;
    </script>
  </body>
</html>