<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>psuh 실습</title>
  </head>
  <body>
    <form action="/getForm" method="post">
      이름 <input type=" text" name="id" required>
      <fieldset>
        <legend>성별</legend>
        <input type="radio" name="gender" value="male" required> 남자
        <input type="radio" name="gender" value="female"> 여자
      </fieldset>
      <fieldset>
        <legend>생년월일</legend>
        <select name="year">
          <% for (let i = 1970; i <=2023; i++){ %>
          <option><%= i %></option>
          <% } %>
        </select>년
        <select name="month">
          <% for (let i = 1; i <=12; i++){ %>
          <option><%= i %></option>
          <% } %>
        </select>월
        <select name="day">
          <% for (let i = 1; i <=31; i++){ %>
          <option><%= i %></option>
          <% } %>
        </select>일
      </fieldset>
      <fieldset>
        <legend>관심사</legend>
        <label><input type="checkbox" name="interest" value="travel">여행</label>
        <label><input type="checkbox" name="interest" value="fashion">패션</label>
        <label><input type="checkbox" name="interest" value="food">음식</label>
      </fieldset>
      <fieldset>
        <legend>선택</legend>
        좋아하는 색상 <input type="color" name="color"><br />
        좋아하는 숫자 <input type="range" name="range" min="1" max="100">
      </fieldset>
      <br />
      <button type="submit">회원가입</button>

    </form>
  </body>
</html>